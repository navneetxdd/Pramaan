M4 — Real acquisition depth**
- Physical disk enumeration + imaging (not just file upload), bad-sector handling, resumable checkpoints, E01/pyewf, read-only enforcement, drift calibration UI.
- **Exit criteria:** a real attached USB/SATA test drive can be imaged end-to-end on Windows, survives a simulated interruption (kill the process mid-image) and resumes correctly, and the resulting hash matches a re-hash of the completed image.

**M5 — Case Export/Import + AI Analytics**
- Signed case-bundle export/import (Part G.11).
- YOLOX-Nano/YuNet/motion detection, 1fps sampling, findings on the timeline.
- **Exit criteria:** a case exported from one machine imports cleanly on a second clean install with all hashes re-verified; AI findings appear as clickable timeline markers with the mandatory "investigative leads only" banner visible.

**M6 — Packaging & Release**
- Dynamic port (if not already done in M1), PyInstaller freeze verified on all three OSes in CI, code signing (Windows Authenticode + macOS notarization), full Part I.4 release checklist, E2E test suite (Section F).
- **Exit criteria:** signed installers exist for Windows/macOS/Linux, each installs and uninstalls cleanly on a fresh VM, the Tool Verification Suite passes 100% on the exact frozen build, and a network-monitoring pass shows zero outbound connections.

---

## D. UI Revamp Specification (desktop app scope, Tauri-first)

### D.1 Information architecture

```
Pramaan
├── /cases                         (Case Dashboard — no case context)
│    └── /cases/:caseId            (Case Overview — step nav, status summary)
│         ├── /acquire
│         ├── /device-id
│         ├── /recover
│         ├── /timeline
│         ├── /custody
│         ├── /report
│         └── /ai-analytics
├── /tool-verification             (app-level, no case context)
└── /settings                      (app-level, no case context)
```

React Router v6, nested under a `<CaseProvider>` that loads the case + devices once and exposes them to every child route — eliminates the current pattern of each page independently fetching case state.

### D.2 Component inventory

**New (install this milestone):**
- `shadcn/ui`: `button`, `dialog`, `sheet`, `tabs`, `tooltip`, `table`, `badge`, `command`, `separator`, `scroll-area`, `dropdown-menu`, `select`, `input`, `label`, `switch`.
- `Bklit UI`: line/area chart (Timeline), ring chart (Recovery confidence breakdown).
- `Sonner` (toasts).
- `@tanstack/react-virtual` (recovered-sequences table, custody ledger, hex inspector when built).
- `cmdk` (via shadcn's `Command` primitive) for the command palette.

**New forensic components, MVP-for-demo first:**
1. `ConfidenceBadge` (trivial, high signal — build first)
2. `HashVerifyBadge` (trivial, event-driven — build first)
3. `ChainLinkIndicator` (status-bar icon, three states — build first)
4. `ScanSweep` (disk-scan progress element — build second, needs SSE wiring done)
5. `RadarSweep` (manufacturer-detection element — build second, same dependency)
6. `HexInspector` + `PartitionTree` (Device Identification's three-pane view — build last within this milestone; highest effort, lowest demo-blocking risk since Device Identification can ship as a simpler two-pane view first and gain the hex inspector in a follow-up pass if time is short)

**Keep:** `TimelineView.tsx` (retarget to Bklit-based rendering, keep the data-fetching logic), `VideoPreview.tsx`, `StatusBadge.tsx` (merge into `ConfidenceBadge` if overlapping), `PageHeader.tsx`, `useOnboardingTour.ts`.

**Delete (final list, execute in M1):**
```
frontend/src/components/ui/BackgroundBeams.tsx
frontend/src/components/ui/SpotlightPanel.tsx
frontend/src/components/ui/MovingBorder.tsx
frontend/src/components/forensic/ForensicAtmosphere.tsx
frontend/src/components/forensic/GridBackdrop.tsx
frontend/src/components/forensic/VendorMarquee.tsx
frontend/src/components/forensic/MetricChart.tsx
frontend/src/components/assistant/ForensicCopilot.tsx
frontend/src/context/