# Reporting module

JSON and HTML forensic case reports with custody chain validation. PDF export for examiner handoff.

**Endpoints:** `GET /api/cases/{id}/report`, `GET /api/cases/{id}/report.html`, `GET /api/cases/{id}/report.pdf`
